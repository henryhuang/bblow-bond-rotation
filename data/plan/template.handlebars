- 轮动参考数据

  - 中间价 {{ midPrice }}
  - 双低均值 {{ dblowAvg }}
    > 大于170退出
  - 双低合格数 {{ dblowValidSize }}
    > 小于130的数量，没有小于130的则退出

- 买卖

  - 卖出
  {{#each sellItems}}
    - {{ this }}
  {{/each}}

  - 买入
  {{#each buyItems}}
    - {{ this }}
  {{/each}}

- 双低筛选名单

  - 新名单 {{ newDataDate }}

{{ newDataContent }}

  - 旧名单 {{ oldDataDate }}

{{ oldDataContent }}
